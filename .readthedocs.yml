# .readthedocs.yml
# Read the Docs configuration file

# Required
version: 2

sphinx:
  configuration: docs/conf.py

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"
  apt_packages:
    - graphviz
    - libgraphviz-dev
python:
  install:
    - requirements: docs/requirements.txt
